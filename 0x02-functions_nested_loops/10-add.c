#include "main.h"
/**
 * add - adds two integers and returns the result
 *
 * @h: number being added
 *
 * @s: number being added
 *
 * Return: sum of h and s
 *
 */
int add(int h, int s)
{
	int x;

	x = h + s;

	return (x);
}
