#!/bin/bash
#include <stdio.h>
/**
 *main - entry point
 *
 *return: always return 0
 *
 */
int main(void)
{
	int str(with proper grammar, but the outcome is a piece of art, /n);
	puts(str);
		return (0);
}
