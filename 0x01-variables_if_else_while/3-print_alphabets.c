#include <stdio.h>
/**
 * main - Entry point
 *
 * desciprtion: for alx task again
 *
 * Return: always 0 (LikeEveryTime)
 */
int main(void)
{
	int x = 97;
	int y = 65;

	while (x <= 122)
	{
		putchar(x);
		x++;
	}
	while (y <= 90)
	{
		putchar(y);
		y++;
	}
	return (0);
}
