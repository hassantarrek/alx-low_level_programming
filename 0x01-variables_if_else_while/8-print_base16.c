#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: for alx task
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int x = 0;

	while (x <= 15)
	{
		putchar(x);
		x++;
	}
	putchar('\n');
	return (0);
}
