#include <stdio.h>
/**
 * main - Entry point
 *
 * description: like every time lol
 *
 * Return: always 0 (lol again)
 *
 */
int main(void)
{
	int x = 0;

	while (x <= 9)
	{
		putchar(x);
		x++;
		}
	putchar('\n');
	return (0);
}
